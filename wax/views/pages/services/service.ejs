(function () {
    angular
        .module("<%= applicationName %>")
        .service("wax<%= pageName %>Service", wax<%= pageName %>Service);

    function wax<%= pageName %>Service($http) {
        this.create<%= pageName %> = create<%= pageName %>;
        this.findAll<%= pageName %> = findAll<%= pageName %>;
        this.find<%= pageName %>ById = find<%= pageName %>ById;
        this.update<%= pageName %> = update<%= pageName %>;
        this.delete<%= pageName %> = delete<%= pageName %>;

        function create<%= pageName %>() {

        }
        function findAll<%= pageName %>() {
            var url = "/wax/api/<%= pageName %>";
            return $http.get(url)
                then(function(response){
                    return response.data;
                });
        }
        function find<%= pageName %>ById(<%= pageName %>Id) {
            var url = "/wax/api/<%= pageName %>/" + <%= pageName %>Id;
            return $http.get(url)
                then(function(response){
                    return response.data;
                });
        }
        function update<%= pageName %>() {

        }
        function delete<%= pageName %>() {

        }
    }
})();